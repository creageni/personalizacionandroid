<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="TxtTitulo" 
        parent="@android:style/Widget.TextView">        
        <item name="android:textAppearance">@color/txt_titulo</item>
        <item name="android:textSize">@dimen/size_titulo</item>
    </style>
    
    <style name="TxtAutor" 
        parent="@android:style/Widget.TextView">        
        <item name="android:textAppearance">@color/txt_autor</item>
        <item name="android:textSize">@dimen/size_author</item>
    </style>

    <style name="TxtPagina" 
        parent="@android:style/Widget.TextView">        
        <item name="android:textAppearance">@color/txt_paginas</item>
        <item name="android:textSize">@dimen/size_paginas</item>
    </style>
    
    
    
    
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>